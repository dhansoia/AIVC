import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export const metadata = { title: 'National Network — India Map' }

export default function Page() {
  return (
    <PagePlaceholder
      title="National Network — India Map"
      eyebrow="Network"
      description="Live state-by-state availability across all 28 states & 8 UTs."
      session="Session 4"
    />
  )
}
