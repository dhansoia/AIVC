import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export const metadata = { title: 'Apply — State Partner' }

export default function Page() {
  return (
    <PagePlaceholder
      title="Apply — State Partner"
      eyebrow="State Partner"
      description="Multi-step State Partner registration form."
      session="Session 5"
    />
  )
}
