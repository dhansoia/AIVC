import { PagePlaceholder } from '@/components/shared/PagePlaceholder'

export const metadata = { title: 'Events' }

export default function Page() {
  return (
    <PagePlaceholder
      title="Events"
      eyebrow="Media"
      description="Upcoming launches, exhibitions, conferences, and roadshows."
      session="Session 8"
    />
  )
}
