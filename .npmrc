# Some Radix UI / charting packages haven't updated their
# peerDependencies to React 19 yet, even though they work fine
# at runtime. Use legacy-peer-deps so npm warns instead of
# failing the install on these stragglers.
legacy-peer-deps=true
